import { Container } from '../../styles/components/Home/ContentGamesStyled';

const ContentGames: React.FC = props => {
  return <Container>{props.children}</Container>;
};

export default ContentGames;
